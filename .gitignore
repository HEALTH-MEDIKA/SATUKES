# CodeIgniter is intentionally committed so a cPanel deployment made from the
# repository works even when Composer/SSH is unavailable on the hosting account.
/application/cache/*
!/application/cache/index.html
/application/logs/*
!/application/logs/index.html
/.env
/*.sql.gz
/.phpunit.result.cache
